- content_for(:page_actions) do
  = link_to(translate(:"titles.callouts.new"), new_dashboard_callout_path, class: 'btn btn-outline-success')

- content_for(:resources_table) do
  = render("shared/resources_table", resource_class: Callout, columns: [:status, :trigger_method, :province, :created_at], unsortable_columns: [:trigger_method, :province])

= render("shared/index")
