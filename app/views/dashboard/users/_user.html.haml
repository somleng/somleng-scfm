= content_tag_for(:tr, user) do
  %td
    = link_to(user.id, dashboard_user_path(user))
  %td
    = user.email
  %td
    = local_time(user.invitation_accepted_at)
  %td
    = local_time(user.last_sign_in_at)
  %td
    = local_time(user.created_at)
