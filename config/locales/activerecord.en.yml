---
en:
  activemodel:
    attributes:
      callout_summary:
        busy_calls: Busy calls
        completed_calls: Completed calls
        errored_calls: Errored calls
        failed_calls: Failed calls
        not_answered_calls: Not answered calls
        participations: Participants
        participations_still_to_be_called: Participants still to be called
      key_value_field:
        key: Key
        value: Value
    models:
      event/base:
        one: Event
        other: Events
  activerecord:
    attributes:
      access_token:
        permissions: Permissions
        token: API key
      account:
        platform_provider_name: Platform provider
      phone_call:
        remote_call_id: Remote call sid
    models:
      access_token:
        one: API key
        other: API keys
      batch_operation/base:
        one: Batch operation
        other: Batch operations
      batch_operation/callout_population:
        one: Callout population
        other: Callout populations
      batch_operation/phone_call_create:
        one: Create phone calls
        other: Create phone calls
      batch_operation/phone_call_queue:
        one: Queue phone calls
        other: Queue phone calls
      batch_operation/phone_call_queue_remote_fetch:
        one: Fetch phone call status
        other: Fetch phone call status
      callout:
        one: Callout
        other: Callouts
      callout_participation:
        one: Callout participation
        other: Callout participations
      contact:
        one: Contact
        other: Contacts
      phone_call:
        one: Phone call
        other: Phone calls
      remote_phone_call_event:
        one: Phone call event
        other: Phone call events
      user:
        one: User
        other: Users
  attributes:
    call_flow_logic: Call flow
    created_at: Created at
    metadata: Metadata
    msisdn: Phone number
    status: Status
