en:
  activerecord:
    attributes:
      access_token:
        token: "API Key"
      contact:
        msisdn: "Phone Number"
      callout:
        voice: "Voice file"
      user:
        location_ids: "Provinces"
      sensor:
        sensor_rules: 'Rules'
      sensor_rule:
        voice: "Voice file"
        level: "Level"
    models:
      batch_operation/callout_population: "Callout Population"
  activemodel:
    attributes:
      key_value_field:
        key: "Key"
        value: "Value"
    models:
      event/base: "Event"
