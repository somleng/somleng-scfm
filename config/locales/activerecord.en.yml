en:
  activerecord:
    attributes:
      access_token:
        token: "API Key"
      contact:
        msisdn: "Phone Number"
      callout:
        voice: "Voice file"
      user:
        location_ids: "Provinces"
    models:
      batch_operation/callout_population: "Callout Population"
  activemodel:
    attributes:
      key_value_field:
        key: "Key"
        value: "Value"
    models:
      event/base: "Event"
