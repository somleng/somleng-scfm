---
en:
  activerecord:
    errors:
      messages:
        file_size: maximum size is %{size}
        file_type: can only be an audio file
      models:
        access_token:
          attributes:
            base:
              restrict_destroy_status: Cannot delete record because the destroyer is not the creator
        phone_call:
          attributes:
            base:
              restrict_destroy_status: Cannot delete record because the destroyer is not the creator
